# =============================================================================
# ServiceAccount whose token the External Secrets Operator uses to authenticate
# to Vault (Kubernetes auth). Vault binds THIS SA + namespace to a Vault role
# and policy (see docs/vault-external-secrets.md).
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-vault
  namespace: traefik
